// const express = require('express');
// const upload = require('../middleware/imageMiddelware'); 
// const router = express.Router();

// router.post('/upload', upload.single('file'), (req, res) => {
//     // Xử lý logic sau khi tệp tin được tải lên
//     const file = req.file; // Thông tin về tệp tin đã được lưu trữ
//     res.status(200).json({ message: 'Tệp tin đã được tải lên thành công', file: file });
// });

// module.exports = router;
